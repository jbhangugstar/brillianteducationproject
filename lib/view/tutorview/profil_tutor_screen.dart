import 'package:flutter/material.dart';

class ProfilTutorScreen extends StatelessWidget {
  const ProfilTutorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Profil Tutor", style: TextStyle(fontSize: 22)),
    );
  }
}
