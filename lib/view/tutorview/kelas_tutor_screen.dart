import 'package:flutter/material.dart';

class KelasTutorScreen extends StatelessWidget {
  const KelasTutorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Kelas Saya", style: TextStyle(fontSize: 22)),
    );
  }
}
